# Copy to .env.myskulboot (the live project id) and fill in the real values.
# Applied automatically by `firebase deploy --only functions` for gen2 functions.
PAYSTACK_SECRET_KEY=sk_live_xxxxxxxx
SMTP_PASSWORD=SG.xxxxxxxx
